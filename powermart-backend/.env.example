# ===========================================
# PowerMart Backend Environment Variables
# ===========================================

# Application
APP_NAME="PowerMart"
FRONTEND_URL="http://localhost:3000"

# Database
DATABASE_URL="postgresql://user:password@localhost:5432/powermart?schema=public"

# JWT Configuration
JWT_SECRET="your-super-secret-jwt-key-change-in-production"
JWT_EXPIRES_IN="7d"

# Server Configuration
PORT=3000
NODE_ENV="development"

# ===========================================
# Email Configuration (Nodemailer SMTP)
# ===========================================
# For Gmail, use:
#   SMPT_HOST=smtp.gmail.com
#   SMPT_PORT=465
#   SMPT_MAIL=your-email@gmail.com
#   SMPT_PASSWORD=your-app-password (not regular password)
#
# For other providers, check their SMTP settings

SMPT_HOST="smtp.gmail.com"
SMPT_PORT="465"
SMPT_MAIL="your-email@gmail.com"
SMPT_PASSWORD="your-app-password"

# ===========================================
# WhatsApp Configuration (Self-hosted - FREE)
# ===========================================
# Uses Baileys library - WhatsApp Web protocol
# No API keys or paid services needed!
#
# First-time setup:
# 1. Start the server: npm run dev
# 2. Scan QR code shown in terminal with WhatsApp
# 3. Session is saved in ./whatsapp-session/
# 4. Subsequent starts will auto-connect
#
# Security:
# - Session files contain auth credentials
# - ./whatsapp-session/ is in .gitignore
# - Keep session folder secure in production

# ===========================================
# Cloudinary Configuration
# ===========================================
# Get these values from your Cloudinary Dashboard:
# https://console.cloudinary.com/settings/api-keys

CLOUDINARY_CLOUD_NAME="your-cloud-name"
CLOUDINARY_API_KEY="your-api-key"
CLOUDINARY_API_SECRET="your-api-secret"

# ===========================================
# Notes on Cloudinary Security
# ===========================================
# 
# PUBLIC ASSETS (products, avatars, logos):
# - Uploaded with type: 'upload' (default)
# - Accessible via public URL without authentication
# - Use for: product images, shop logos, user avatars, category images
#
# PRIVATE ASSETS (KYC documents, vendor docs):
# - Uploaded with type: 'private' or 'authenticated'
# - Requires signed URL with expiration to access
# - Only the owner or admin can request signed URLs
# - Use for: KYC documents, sensitive vendor documents
#
# To enable strict access control on Cloudinary:
# 1. Go to Cloudinary Dashboard > Settings > Security
# 2. Enable "Strict transformations"
# 3. Enable "Signed uploads" if needed
# 4. Configure Media Library access restrictions
